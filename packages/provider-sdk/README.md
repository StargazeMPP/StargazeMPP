# `packages/provider-sdk` — `@stargazempp/provider-sdk`

**Owner:** shared. External dev owns the developer-facing decorator API and ergonomics; this team owns the voucher validation primitives (EIP-712 ecrecover, ZK proof helpers) that the SDK wraps.

Goal: any HTTP endpoint becomes a monetized MPP intelligence service with one decorator. See the overview PDF section 8.
