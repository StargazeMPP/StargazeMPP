[package]
name = "vault-verifier-core"
version = "0.0.0"
edition = "2021"
license = "Apache-2.0"
description = "Groth16 verifier helpers for per-circuit Anchor verifier programs."

[lib]
name = "vault_verifier_core"

[dependencies]
groth16-solana = "0.2.0"
solana-program = "=2.3.0"
thiserror = "2"
