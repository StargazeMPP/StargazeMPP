{
  "name": "@stargazempp/vault-circuits",
  "version": "0.0.0",
  "private": true,
  "description": "Groth16 circuits + on-chain verifiers for StargazeVault privacy tiers.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "circuits"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "compile:aggregate": "circom circuits/aggregate_sum.circom --r1cs --wasm --sym -l node_modules -l ../../node_modules -o build/",
    "setup:aggregate-dev": "node scripts/setup-dev.mjs aggregate_sum",
    "verifier:aggregate": "snarkjs zkey export solidityverifier build/aggregate_sum_final.zkey build/AggregateSumVerifier.sol",
    "all:aggregate-dev": "npm run compile:aggregate && npm run setup:aggregate-dev && npm run verifier:aggregate",
    "compile:geofence": "circom circuits/geofence.circom --r1cs --wasm --sym -l node_modules -l ../../node_modules -o build/",
    "setup:geofence-dev": "node scripts/setup-dev.mjs geofence",
    "verifier:geofence": "snarkjs zkey export solidityverifier build/geofence_final.zkey build/GeofenceVerifier.sol",
    "all:geofence-dev": "npm run compile:geofence && npm run setup:geofence-dev && npm run verifier:geofence"
  },
  "dependencies": {
    "@stargazempp/shared": "*",
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.5"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.5.0",
    "vitest": "^4.1.0"
  }
}
