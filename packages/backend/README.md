# `packages/backend` — Express + tRPC + Reputation Oracle

**Owner:** external dev.

The HTTP API surface for StargazeMPP: provider registry, MPP session lifecycle, tRPC routes, Reputation Oracle. Talks to Solana / Tempo via the modules in `packages/contracts-evm`, `packages/anchor-program`, `packages/indexer`, and `packages/vault-circuits` — but doesn't implement them.

See `EXTERNAL_DEV_BRIEF.md` at the repo root for the full task list.
